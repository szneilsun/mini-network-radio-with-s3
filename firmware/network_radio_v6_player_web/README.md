# 网络收音机 V6：网页播放器控制台

V6 在 V5 的 HLS/AAC 网络音频播放基础上，提供可直接使用的网页播放器界面：

- 播放、停止、音量（0–21）控制；
- 每两秒刷新播放状态、缓冲状态、采样率和码率；
- 点击某个电台的“播放此台”会保存默认台并立即切换；
- 保留 Wi-Fi 配网和播放列表增删改排序。

硬件仍使用 MAX98357A：GPIO4/BCLK、GPIO5/LRCLK、GPIO6/DIN。开发阶段热点为 `Radio-1FB8`，密码 `radio-setup`，访问 `http://192.168.4.1`。
