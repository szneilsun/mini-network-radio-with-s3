# Changelog

本文件记录用户可见功能、兼容性、修复、硬件要求和升级方式的变化。每次修改固件版本号时，必须新增对应版本条目；开发中尚未发布的变化先写入“未发布”。

## 未发布

暂无。

## 4.4.3 - 2026-09-17

### 修复

- I²S 音频输出改为在已连上路由器并真正开始播放时才初始化，避免启动及配网阶段因遗留 I²S 通道而重启循环；配网热点和管理网页可稳定启动。

## 4.4.2 - 2026-09-17

### 修复

- 暂停自动播放开机提示音：当前 ESP32-audioI2S 的文件解码初始化会与后续运行期 I²S 驱动冲突并导致重启，优先恢复网络电台的稳定启动。提示音资源仍保留，待采用独立播放路径后再启用。

## 4.4.1 - 2026-09-17

### 修复

- 启动音与软件重启后先复位 ESP32-S3 的 I²S 外设，避免 ESP32-audioI2S 报告通道已占用并触发重启循环。

## 4.4.0 - 2026-09-17

### 新增

- 管理后台新增 LittleFS 资源 OTA：可无线上传构建产物 `littlefs.bin`，更新台标、开机提示音等资源。

### 升级说明

- 先通过既有“固件升级”上传应用 `.bin`，设备重启后再上传 `littlefs.bin`；资源升级不影响 Wi-Fi、电台、管理密码及其他 NVS 设置。

## 4.3.0 - 2026-09-17

### 新增

- 上电或重启时从 LittleFS 播放约 4 秒的低音量原创开机提示音，结束后自动开始网络连接与电台播放。

## 4.2.0 - 2026-09-17

### 改进

- Wi-Fi 断线恢复改为非阻塞状态机：异步扫描、逐个尝试已保存网络，不再阻塞网页控制、DNS 或状态灯刷新。
- 自动恢复期间阻止手动 Wi-Fi 扫描与连接流程相互打断。

## 4.1.0 - 2026-09-17

### 新增

- 配网后台可保存最多 5 组 Wi-Fi 凭据，支持查看、更新和单独删除网络。
- 启动时扫描周边网络，优先连接信号最强的已保存网络；若连接失败会依次尝试其他已保存网络及隐藏 SSID。

### 兼容性

- 自动迁移旧版单 Wi-Fi 配置，升级后无需重新配网。

## 4.0.0 - 2026-09-16

### 修复

- Wi-Fi 扫描前自动启用 STA 接口，解决首次配网时 AP-only 模式导致扫描失败的问题。
- 扫描进行期间暂停 Wi-Fi 自动重连，避免扫描被 `WiFi.reconnect()` 中断。

### 维护

- 将项目整理为固定的 `sources/`、`docs/`、`build/`、`release/` 和 `tools/` 目录结构。
- 源码工程固定为 `esp32-network-radio`，后续版本直接更新，不再建立版本目录。
- 删除历史固件快照、旧构建产物和过期发布包，只保留当前源码。
- 合并项目说明，补充硬件接线、Flash 分区和恢复出厂后的配置流程。

## 3.0.2

### 修复

- 统一播放列表文件读写使用的数量字段宽度。
- 修复全新 Flash 首次启动时 LittleFS 播放列表校验失败并降级到 NVS 的问题。

### 改进

- 恢复宽松的管理页面布局、大尺寸播放与电台操作按钮以及分列音量显示。
- 保留接口安全校验、节流轮询、台标占位图、诊断、OTA 和播放恢复能力。
- 保持既有 NVS 键、播放列表格式、Wi-Fi 配置和 LittleFS 台标路径兼容。
